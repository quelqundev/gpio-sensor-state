{
  "name": "gpio-sensor-state",
  "version": "1.0.0",
  "description": "This library allow to get a sensor GPIO state and to be notified on state change",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build/ && tsc",
    "start": "npm run build && node build/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/quelqundev/gpio-sensor-state.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/quelqundev/gpio-sensor-state/issues"
  },
  "dependencies": {
    "lodash": "^4.17.20",
    "onoff": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.10.1"
  }
}
